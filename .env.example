# Deployment via foundry accounts
ACCOUNT_NAME=

# Alchemy API key — used as a multichain RPC for tests, scripts and fork tests.
# foundry.toml wires it into every chain's RPC endpoint.
ALCHEMY_API_KEY=

# Etherscan v2 API key
ETHERSCAN_API_KEY=
